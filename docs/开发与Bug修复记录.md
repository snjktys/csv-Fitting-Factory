# 开发与 Bug 修复记录

## 1. 开发阶段记录

### 阶段一：需求与设计核对

- 阅读课程指导书第 38 题；
- 核对需求分析文档；
- 明确四种模型、智能初值、两子图、R²、RMSE 和 TXT 报告；
- 确定不使用数据库，采用本地 CSV 数据流。

阶段自检：需求分析、系统设计和课程题目一致，没有引入登录、云端等无关功能。

### 阶段二：底层模型与数据处理

- 建立统一异常和数据对象；
- 完成 CSV 读取、中文编码、数据清洗；
- 完成三种误差模式；
- 完成四种模型公式；
- 准备四份示例 CSV。

阶段自检：代码可编译，“读取 CSV→清洗→模型校验”冒烟路径通过。

### 阶段三：核心拟合能力

- 完成四种智能初值；
- 完成 `curve_fit`、参数边界、残差、R² 和 RMSE；
- 完成参数误差、绘图和 TXT 报告；
- 建立自动化测试。

阶段自检：25 项测试通过，四类示例数据 R² 均大于 0.95。

### 阶段四：GUI 与整体联调

- 完成 Tkinter 主界面；
- 接入列选择、误差模式、模型、参数和智能初值；
- 接入拟合图、残差图、数据预览和报告保存；
- 增加配置变化后旧结果失效逻辑。

阶段自检：27 项测试通过，GUI 窗口创建成功。

### 阶段五：测试和提交材料

- 运行四种模型端到端测试；
- 生成 README、AI 使用说明、测试报告和本开发记录；
- 核对课程提交清单和项目结构。

阶段自检：27 项测试通过，GUI 窗口创建成功。

### 阶段六：随机与异常数据测试

- 使用固定随机种子生成 15 份可复用测试 CSV；
- 覆盖噪声、缺失值、非数字、Inf、重复 X、乱序、常量 Y、非法 sigma、GB18030 和非法文件；
- 将测试数据保存到 `tests/test_data`，由本人用于复测；
- 运行全部测试并记录每类场景结果。

阶段自检：46 项测试全部通过，没有发现业务代码缺陷。

## 2. Bug 修复记录

### 测试脚本兼容性问题：Pandas 3 禁止向 float64 列写入字符串

- 现象：随机测试准备“缺失”字符串时，Pandas 直接抛出类型错误；
- 原因：Pandas 3 对隐式类型升级更严格，float64 列不能直接混入字符串；
- 修复：在测试数据构造阶段先将该列转换为 `object`，再写入异常字符串；
- 结果：测试成功覆盖真实的非数字清洗场景，业务代码无需修改。

### Bug 1：中文图表字体缺失

- 现象：Matplotlib 测试出现大量中文字形缺失警告；
- 原因：默认 DejaVu Sans 不包含完整中文；
- 修复：按优先级选择 Microsoft YaHei、SimHei、Noto Sans CJK SC；
- 结果：测试重新通过且无字体警告。

### Bug 2：数据点规则存在歧义

- 现象：设计文档中“总点数”和“唯一 X”条件表述不同；
- 修复：总点数必须大于参数数，唯一 X 必须不少于参数数；
- 结果：规则已写入设计、代码和测试。

### Bug 3：指数模型可能产生伪结果

- 现象：如果直接裁剪指数输入，虽然不溢出，却改变了数学模型；
- 修复：不静默裁剪；根据 X 尺度限制 `b`，溢出时明确失败；
- 结果：模型含义保持正确。

### Bug 4：旧拟合结果与新配置不一致

- 现象：用户修改列或参数后，旧报告可能不再对应当前设置；
- 修复：任何配置变化都会清空图形、指标和报告状态；
- 结果：只能导出最新成功拟合的结果。

### Bug 5：参数符号缺少公式对应关系（界面检查记录与辅助修正，2026-09-12）

- 发现过程：界面检查记录显示，输入框只有 `a0`、`a1`、`a`、`b` 等字母数字，未标注这些符号在模型公式中的具体含义，手动设置初值时容易误解。
- 原因分析：`ModelSpec` 仅保存参数名，没有保存参数说明；GUI 只渲染参数名，没有展示模型公式。
- 修复内容：
  - 为 `ModelSpec` 增加 `parameter_descriptions` 字段，并为四种模型建立与参数顺序一致的中文说明；
  - 在 GUI 参数区域增加公式显示；
  - 将参数标签改为“符号（含义）”，例如 `a1（x^1 项的系数）`、`k（增长/下降速率）`；
  - 增加模型元数据测试，确认说明数量和顺序与公式一致。
- 复核结果：本人已检查多项式升幂系数顺序、指数模型 `a/b/c`、正弦模型 `A/f/phi/c` 以及 Logistic 模型 `L/k/x0/c` 的对应关系。
- 修复结果：用户在设置初值时可以直接看到公式和参数含义，原有拟合函数参数顺序不变，不影响计算逻辑。
- 验证状态：源码编译、核心元数据检查和完整自动化测试均已由本人运行并通过。

### Bug 6：按钮样式不一致及小窗口底部状态栏消失（界面检查记录与辅助修正，2026-09-12）

- 发现过程：GUI 检查记录显示，“智能初值”使用 ttk 默认按钮样式，而“开始拟合”使用强调样式，字体大小和按钮高度不一致；切换到较小窗口并选择高阶多项式时，参数行增多会使最下面的状态栏消失。
- 原因分析：两个按钮的 style 不统一；左侧控制区没有滚动机制；`main` 主区域先使用 `pack(fill=BOTH, expand=True)`，状态栏最后 pack，导致状态栏被主区域抢占空间。
- 修复内容：
  - 新增 `Action.TButton`，统一“智能初值”和“开始拟合”的字体（Microsoft YaHei UI 10 bold）及内边距；
  - 左侧控制区改为 Canvas + Scrollbar，内容超出可视高度时可滚动；
  - 状态栏改为先 pack，主内容区随后 expand，确保状态栏始终保留固定高度；
  - 多项式参数区改为按内容自然增长，不再强行占满剩余高度。
- 复核结果：本人已在 1050×680 和 1050×400 窗口尺寸下生成 10 阶多项式参数，检查底部状态栏、左侧滚动区域和按钮样式。
- 修复结果：小窗口下状态栏不再消失，参数过多时可以滚动查看；按钮视觉尺寸统一，操作区更易识别。
- 验证状态：实际启动 GUI 冒烟检查通过；Tk 几何检查确认状态栏高度为 33px、10 阶参数滚动区域有效；全部 47 项自动化测试通过。

### Bug 7：左侧滚轮误改下拉框选项且滚动不稳定（界面检查记录与辅助修正，2026-09-12）

- 发现过程：鼠标滚轮检查记录显示，指针在输入量、输出量或误差来源下拉框上方时，滚轮会改变选项；滚动左侧整体区域时又存在时灵时不灵的情况。
- 原因分析：旧代码通过 `bind_all` 绑定滚轮，无法可靠阻止 ttk Combobox/Spinbox 的默认事件处理；滚轮绑定只在部分容器进入/离开时切换，子控件之间传播不一致。
- 修复内容：
  - 为左侧控制区增加专用 bind tag，并递归安装到所有控件的 bindtags 首位；
  - 统一处理 `<MouseWheel>`、`<Button-4>` 和 `<Button-5>` 事件；
  - 滚动左侧 Canvas 后返回 `break`，阻止 Combobox/Spinbox 改变当前值；
  - 动态生成的多项式参数输入框在创建后重新安装滚轮绑定。
- 复核结果：本人已向 X 列 Combobox 发送滚轮事件，确认选项保持原值、左侧 Canvas 正常滚动且右侧图表区域不受影响。
- 修复结果：滚轮只负责滚动左侧控制区，不再意外修改下拉框或数字框参数，左侧各控件位置的滚动行为统一。
- 验证状态：Tk 事件级检查和完整 47 项自动化测试通过；本人已使用实际鼠标复测滚轮上下滚动。

### Bug 8：全屏时仍保留无效滚轮操作（界面检查记录与辅助修正，2026-09-12）

- 发现过程：界面检查记录显示，全屏时左侧参数内容已经全部可见，但滚动容器仍持续接收滚轮，使用体验不够自然。
- 原因分析：旧实现无论内容是否超出视口都显示滚动容器，没有按窗口大小动态判断滚动需求。
- 修复内容：
  - 根据 Canvas `scrollregion` 内容高度和当前视口高度计算 `_controls_scrollable`；
  - 内容完全可见时隐藏滚动条、回到顶部并忽略滚轮；
  - 内容超出视口时显示滚动条并启用滚轮；
  - 窗口大小变化、模型切换和参数行重建后重新计算滚动状态。
- 复核结果：本人已检查 1050×800 + 2 阶多项式时滚动条隐藏、1050×680 + 10 阶多项式时滚动条显示，以及切换模型后的状态更新。
- 修复结果：全屏或大窗口无需滚轮，小窗口和高阶参数仍有明确、可用的滚动能力。
- 验证状态：Tk 几何/事件检查通过；完整 47 项自动化测试通过。

### Bug 9：Matplotlib 工具栏提示为英文（界面检查记录与辅助修正，2026-09-12）

- 发现过程：工具栏检查记录显示，鼠标悬停平移按钮会显示 `Left button pans, Right button zooms` 等英文提示，与程序其他中文界面不一致。
- 原因分析：Matplotlib `NavigationToolbar2Tk` 默认 `toolitems` 使用英文 Tooltip；项目直接实例化默认类，未提供中文本地化文本。
- 修复内容：
  - 在 `src/app.py` 新增 `ChineseNavigationToolbar2Tk` 子类；
  - 覆盖 Home、Back、Forward、Pan、Zoom、Subplots、Save 的 Tooltip 文本为中文；
  - 保留 Matplotlib 原有图标、按钮回调、平移/缩放/保存功能，不改第三方库源码。
- 复核结果：本人已检查平移提示中的左键拖动、右键缩放、X/Y 固定坐标轴和 Ctrl 固定纵横比说明，以及缩放、子图配置和保存提示。
- 修复结果：图表工具栏的悬停说明与主界面语言统一，用户无需阅读英文即可理解操作。
- 验证状态：实际 GUI 创建检查通过，工具栏类为 `ChineseNavigationToolbar2Tk` 且 7 个工具项均有中文提示；完整 47 项自动化测试通过。

### Bug 10：子图配置窗口内部仍为英文（界面检查记录与辅助修正，2026-09-12）

- 发现过程：子图配置检查记录显示，新窗口内部的标题、操作说明、边距/间距滑块和 `Reset` 按钮仍显示英文。
- 原因分析：Matplotlib `SubplotTool` 直接创建英文标签，并通过滑块标签文字拼装 `subplots_adjust` 参数；简单替换文字会把 `left` 等合法参数变成中文而导致功能错误。
- 修复内容：
  - 新增 `ChineseSubplotTool` 子类，中文化窗口说明、六个滑块和重置按钮；
  - 在 `ChineseNavigationToolbar2Tk.configure_subplots` 中创建中文配置窗口；
  - 重写滑块变化处理，使用固定的 `left/bottom/right/top/wspace/hspace` 内部键，不依赖中文显示标签；
  - 新增测试，检查工具栏提示和子图参数中文标签。
- 复核结果：本人已打开配置窗口检查文字，并移动左边距滑块确认目标 Figure 的 `subplotpars.left` 会改变。
- 修复结果：配置子图窗口的用户可见文字全部中文化，滑块调节和重置功能保持原有逻辑。
- 验证状态：配置窗口实际创建和滑块操作检查通过；完整 48 项自动化测试通过。

### 功能改进 11：完整 GUI 中英文双向切换（本人提出，AI 辅助实现，2026-09-17）

- 需求过程：默认显示中文，并能从“帮助 → 界面语言 → English”切换英文；英文状态下可从“Help → Language → 中文”切回；所有用户可见界面同步切换。
- 原因分析：用户可见文本分散在主界面、下拉选项、状态/消息、图表和 Matplotlib 组件中；原来的中文下拉显示值兼作业务查找键，无法安全直接翻译。
- 修复内容：
  - 新增 `src/localization.py`，统一中英文词条、模型/误差显示映射、参数说明及常用提示翻译；
  - 将 Matplotlib 工具栏和子图配置扩展为 `LocalizedNavigationToolbar2Tk`、`LocalizedSubplotTool`；
  - 为 `visualization.py` 增加语言参数，双语绘制空状态、拟合结果、图例和残差图；
  - `app.py` 使用内部稳定键读取模型/误差配置，切换语言时刷新菜单、控件、下拉值、状态和图表；
  - 切换过程保留 CSV、参数输入、智能初值、清洗行数和已有拟合结果。
- 复核结果：真实 Tk 窗口默认中文；切换 English 后窗口标题、菜单、下拉项、按钮、状态栏、图表、工具栏 Tooltip、子图配置标题/说明/滑块/按钮均为英文；沿相同菜单路径切回中文后全部恢复。
- 功能验证：加载 `polynomial_sample.csv`，生成智能初值并完成拟合后双向切换，参数值和 `fit_result` 未改变；新增双语键一致性、工具栏/子图文本和 GUI 双向切换测试。
- 验证状态：完整 `50 passed`，`compileall` 和 `git diff --check` 通过。

### Bug 12：英文参数说明挤压数值输入框（界面检查记录与辅助修正，2026-09-17）

- 发现过程：切换英文界面后，参数符号和较长英文说明挤在同一行，标签列占用过多横向空间，右侧数值框变窄，较长数字可能显示不全。
- 原因分析：参数标签与输入框共用同一 Grid 行；英文说明比中文长，Tk 会按标签请求宽度扩张第一列，从而压缩第二列输入框。
- 修复内容：
  - 中文界面继续使用“符号（含义）+ 输入框”的原单行布局；
  - 英文界面第一行只显示参数符号和数值输入框；
  - 英文说明放到第二行，横跨两列并按 290px 自动换行；
  - 保留原有参数值保存、键盘事件和左侧滚轮绑定逻辑。
- 复核结果：在 1050×680 窗口检查 10 阶多项式、指数、正弦和 Logistic；四种模型的英文输入框最窄为 157px，10 阶多项式 11 个输入框均为 259px，并可通过左侧滚动查看全部参数。
- 修复结果：英文说明可以完整分行显示，不再抢占数值框宽度；切回中文后手工输入的 `123.456` 仍保留。
- 验证状态：新增英文参数布局测试；完整自动化测试、`compileall` 和 `git diff --check` 均通过。

### Bug 13：英文界面的数据列选项仍显示中文（界面检查记录与辅助修正，2026-09-17）

- 发现过程：英文界面截图中，分区标题和 `X Column / Y Column / Error Source` 标签已显示英文，但加载中文表头 CSV 后，下拉列表仍为“输入量、输出量、误差”，数据预览表头也不会随语言变化。
- 原因分析：旧实现直接把 `dataset.column_names` 同时作为界面显示值和 Pandas 取列键；本地化模块只处理程序固定文本，没有为 CSV 列名建立独立显示层。
- 修复内容：
  - 为课程示例中的常见语义表头建立保守的英文显示映射；
  - X、Y、误差来源下拉框使用本地化显示名，拟合时反向映射到原始 CSV 列名；
  - 数据预览表头随中英文切换同步刷新；
  - 未识别的自定义列名保持原样，翻译后若出现重名则保留原名，避免选择歧义。
- 复核结果：英文界面显示 `Input、Output、Error`；内部数据处理仍使用“输入量、输出量、误差”，成功获得 62 行有效数据；切回中文后三个选项恢复且选择不变。
- 修复结果：程序自带的示例语义列名与英文界面一致，同时没有改写 CSV、DataFrame 或报告使用的真实字段。
- 验证状态：新增双向列名显示测试和未知/重名保护测试；完整自动化测试、`compileall` 和 `git diff --check` 均通过。

## 3. 课程要求对应的最终 GUI 验收（2026-09-13）

- 使用真实 Tkinter 主窗口检查启动、布局和初始控件状态；
- 通过 GUI 回调打开四份示例 CSV，分别完成多项式、指数、正弦和 Logistic 拟合；
- 分别验证无误差、CSV 误差列和统一误差值三种模式；
- 验证 GB18030 编码文件、中文表头、数据预览和 TXT 报告导出；
- 模拟取消打开/保存对话框，确认不会改变程序状态；
- 输入非法参数，确认弹出中文错误提示且程序不退出；
- 切换 10 阶多项式并调整窗口大小，确认参数区可滚动、状态栏仍显示；
- 检查中文工具栏提示和子图配置参数标签；
- 同时运行完整自动化测试，结果为 `48 passed`。

验收结论：课程指导书要求的 CSV 导入、数据处理、四种模型、智能初值、曲线拟合、可视化、残差、R²、RMSE、TXT 报告和异常提示均已实际验证通过。未实现的 FFT 多正弦、蒙特卡洛重采样等属于设计文档列出的进阶功能，不属于当前基础版本验收范围。

## 4. 后续可选改进

- 指导书进阶功能：FFT 多正弦叠加；
- 1000 次蒙特卡洛重采样和参数直方图；
- 拟合图导出为 PNG；
- 对超大 CSV 使用工作线程；
- 打包为 Windows 可执行程序。

这些属于进阶或发布功能，当前基础版本不依赖它们即可满足第 38 题核心要求。
