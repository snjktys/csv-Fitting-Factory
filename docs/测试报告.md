# CSV 数据曲线拟合工厂测试报告

## 1. 测试目标

检查程序是否满足需求分析和系统设计中的 Must Have 功能，并保证常见错误不会导致程序崩溃。

## 2. 测试环境

| 项目 | 内容 |
|---|---|
| 操作系统 | Windows |
| Python | 3.14.3 |
| 测试框架 | pytest 9.1.1 |
| 测试日期 | 初始记录：2026-08-26；进阶功能复核：2026-09-18 |

## 3. 自动化测试结果

执行命令：

```powershell
python -m pytest -q
```

结果：

```text
27 passed in 1.57s
```

### 3.1 随机与异常 CSV 测试（2026-09-03）

为避免只测试固定示例，本轮使用固定随机种子生成了 15 份可复用 CSV，统一保存于 `tests/test_data`。固定种子可以让每次复测得到相同数据，便于定位问题和在答辩中说明测试过程。

| 类别 | 文件数量 | 覆盖内容 |
|---|---:|---|
| 随机多项式 | 3 | 不同噪声、中文表头、缺失值、非数字和 Inf |
| 随机指数 | 3 | 无误差列、不同噪声和正负 X 范围 |
| 随机正弦 | 2 | 均匀采样 FFT 初值、非均匀采样降级初值 |
| 随机 Logistic | 1 | 乱序、重复 X、中文列名、误差列 |
| 边界数据 | 4 | 常量 Y、唯一 X 不足、非法误差、指数极端初值 |
| 编码/文件异常 | 2 | GB18030 中文 CSV、非法字节文件 |

新增随机与异常场景后的阶段性结果：

```text
46 passed in 1.26s
```

重点结果：随机多项式、指数、正弦和 Logistic 数据均完成预期拟合；GB18030 文件能正确识别中文表头；非法字节、常量 Y、唯一 X 不足、非法误差和极端指数参数均按设计转换为提示或业务异常。

覆盖内容：

- UTF-8-SIG 中文表头 CSV 读取；
- 空路径和单列 CSV 错误处理；
- 无误差、CSV 误差列、统一误差值；
- 非数字、NaN、Inf 和非正误差清理；
- 数据点总数和唯一 X 数量规则；
- 多项式、指数、正弦、Logistic 公式；
- 四种模型智能初值；
- 四种示例数据真实 `curve_fit`；
- 残差、R² 和 RMSE；
- 常量 Y 时 R² 返回 N/A；
- 参数与 TXT 报告字段；
- Matplotlib 两子图；
- GUI 模块和三种误差模式可导入。

## 4. 四模型端到端测试

端到端流程：读取 CSV → 选择误差列 → 智能初值 → curve_fit → 指标 → TXT 报告。

| 模型 | 示例文件 | R² | RMSE | 结果 |
|---|---|---:|---:|---|
| 二阶多项式 | `polynomial_sample.csv` | 0.999498 | 0.148668 | 通过 |
| 指数 | `exponential_sample.csv` | 0.999930 | 0.057924 | 通过 |
| 正弦 | `sine_sample.csv` | 0.999995 | 0.002987 | 通过 |
| Logistic | `logistic_sample.csv` | 0.999999 | 0.004575 | 通过 |

四份端到端报告已在测试期间成功生成。`output` 属于运行输出，已通过 `.gitignore` 排除，不作为源码的一部分。

## 5. GUI 冒烟测试

测试内容：创建 Tkinter 根窗口、实例化主界面、加载 Matplotlib 画布，然后安全关闭。

结果：

```text
GUI_CREATED CSV 数据曲线拟合工厂 3
```

说明：窗口、主布局和菜单可以正常创建，没有发生导入错误或启动异常。

## 6. 已发现并修复的问题

| 问题 | 修复 |
|---|---|
| Matplotlib 默认字体不支持中文 | 自动选择 Microsoft YaHei、SimHei 等字体 |
| 数据点数量规则曾不统一 | 统一为总点数大于参数数，唯一 X 不少于参数数 |
| 指数可能溢出 | 给 `b` 设置与 X 尺度相关的边界，并捕获溢出 |
| 误差棒来源过于单一 | 增加无误差、CSV 列、统一误差三种模式 |
| 配置改变后可能误用旧结果 | 修改列、误差、模型、阶次或参数时让旧结果失效 |

## 7. 学生本人 GUI 验收记录

本人已在 Windows + Python 3.14.3 环境中，通过实际 Tkinter 窗口和控件回调完成以下手工/半自动验收：

| 验收项 | 结果 | 说明 |
|---|---|---|
| 启动主窗口 | 通过 | 窗口标题、初始布局和控件禁用状态正常 |
| 四种模型拟合 | 通过 | 多项式、指数、正弦、Logistic 均成功完成拟合 |
| 三种误差模式 | 通过 | 无误差、CSV 误差列、统一正数误差均可运行 |
| 中文文件/列名 | 通过 | GB18030 文件可读取，中文表头可选择 |
| 数据预览 | 通过 | 可打开预览窗口，计算仍使用全部数据 |
| 取消打开/保存对话框 | 通过 | 取消后保持当前状态，不触发异常 |
| 错误参数处理 | 通过 | 输入非数字参数时弹出提示，程序不退出 |
| 窗口缩放与高阶参数 | 通过 | 10 阶多项式参数区可滚动，状态栏仍可见 |
| 中文工具栏/子图配置 | 通过 | 工具提示、滑块标签和重置按钮均为中文 |
| TXT 报告导出 | 通过 | 四种模型报告均成功生成并可保存 |

本轮实际测试命令和结果：

```text
python -m pytest -q
48 passed
GUI_E2E_OK
GUI_EDGE_OK
```

以下清单记录本人已完成的复测项目：

- [x] 从 GUI 打开四份示例 CSV；
- [x] 分别完成四种模型拟合；
- [x] 测试三种误差模式；
- [x] 测试中文文件名和中文列名；
- [x] 测试取消打开和保存对话框；
- [x] 测试错误参数、空数据、非数字数据；
- [x] 检查窗口缩放、图例和中文字体；
- [x] 保存并打开 TXT 报告；
- [x] 现场修改一个小功能并重新运行测试。

## 8. 测试结论

基础版本当时的自动化测试、四模型端到端测试、GUI 创建检查和实际 GUI 验收均通过；该阶段复测结果为 `48 passed`。其后新增的蒙特卡洛进阶功能验证单独记录如下。

## 9. 蒙特卡洛进阶功能测试（2026-09-18）

实现指导书第 38 题进阶要求：“对拟合参数跑 1000 次重采样，画直方图”。

| 测试项 | 预期结果 | 实际结果 |
|---|---|---|
| 固定种子执行 20 次重采样 | 返回 20 行、每列对应一个模型参数 | 通过，二阶多项式返回 `(20, 3)` |
| 参数样本有限性 | 所有成功样本均为有限数值 | 通过 |
| 非法重采样次数 | 次数为 0 时抛出参数业务异常 | 通过 |
| 参数直方图 | 每个参数对应一个子图 | 通过，3 个参数生成 3 个子图 |
| 指导书规定的 1000 次运行 | 返回 1000 组参数样本 | 通过，返回 `(1000, 3)` |

验证命令与结果：

```text
python -m pytest -q tests/test_csv_handler.py tests/test_data_processor.py tests/test_fitting_models.py tests/test_initial_guess.py tests/test_fitting_engine.py tests/test_random_csv_cases.py tests/test_report_generator.py tests/test_visualization.py tests/test_monte_carlo.py
49 passed

python -m pytest -q tests/test_monte_carlo.py tests/test_fitting_engine.py tests/test_visualization.py
10 passed
```

性能抽查：`polynomial_sample.csv` 二阶多项式执行 1000 次有放回重采样，在当前环境耗时约 `0.273 s`。

完整测试说明：执行自动化测试时，当前 Python 3.14 的 Tk 安装缺少 `ttk/altTheme.tcl`，导致一个原有 GUI 用例无法创建根窗口；其余测试通过。随后本人已在可正常运行 Tk 的程序环境中完成现场操作复核，确认蒙特卡洛按钮、状态提示和参数直方图窗口工作正常。

### 9.1 多参数直方图布局回归测试

针对本人发现的高阶多项式参数图重叠问题，使用 11 个参数构造直方图并检查：

- 生成 11 个参数子图；
- 自动排列为 `4×3` 网格；
- 启用 Matplotlib 约束布局；
- 限制每个 X 轴的主刻度数量；
- 实际渲染检查中，参数标题、坐标轴文字和刻度没有重叠。

相关测试结果：`6 passed`。本人已在 GUI 中完成人工复核。
