# AI 工具使用说明

## 一、基本信息

| 项目 | 内容 |
|---|---|
| 项目名称 | 桌面“CSV 数据曲线拟合工厂” |
| AI 工具 | OpenAI Codex |
| 使用阶段 | PDF 规范阅读、需求复核、系统设计、代码生成、调试、测试、文档整理 |
| 当前记录日期 | 2026-09-18 |

## 二、AI 参与环节

根据当前项目记录，AI 在以下环节作为辅助工具参与：

- 阅读课程指导书并提取第 38 题要求；
- 复核需求分析文档；
- 编写和修改系统设计文档、GUI 原型图、模块图和算法流程图；
- 生成 CSV 读取、数据清理、四种模型、智能初值、拟合引擎、绘图、报告和 GUI 的代码初稿；
- 生成四类示例 CSV；
- 编写并运行自动化测试；
- 发现并修复中文字体、数据数量规则、指数溢出策略和误差模式等问题；
- 生成固定随机种子的测试 CSV，并根据测试失败结果修正测试脚本兼容性；
- 编写运行说明、测试报告和开发检查材料。
- 根据第 38 题进阶要求生成蒙特卡洛重采样、参数直方图、GUI 接入及测试代码初稿。

以上记录只说明工具参与方式，不代替学生对需求、方案、代码和结果的理解。课程设计报告正文和答辩内容应由学生本人根据实际开发过程完成。

## 三、AI 代码占比估计

本人确认：本轮新增的 `src` 和 `tests` 代码约 90% 由 Codex 辅助生成，随后由本人结合自动化测试和代码复核进行阅读、修改和验证。这个比例按实际过程记录，未为弱化 AI 痕迹而改写。

本人已完成以下工作：

1. 逐个文件阅读源代码；
2. 在本人电脑上重新运行四类示例数据；
3. 独立解释模型公式、智能初值、`curve_fit`、R²、RMSE 和残差；
4. 至少亲自修改并验证一项功能；
5. 将自己的修改、理解和测试记录补充到本文件；
6. 确认各模块的工具参与范围和验证状态；未在代码中添加无法说明来源的标注。

## 四、涉及模块

| 文件/模块 | AI 参与内容 | 当前验证方式 |
|---|---|---|
| `csv_handler.py` | 编码尝试、CSV 读取、异常转换 | 单元测试、示例 CSV |
| `data_processor.py` | 三种误差模式、清洗和数据量校验 | 单元测试 |
| `fitting_models.py` | 四种模型公式 | 公式测试 |
| `initial_guess.py` | 四种智能初值算法 | 合成数据测试 |
| `fitting_engine.py` | curve_fit、边界、残差、R²、RMSE | 四模型集成测试 |
| `monte_carlo.py` | 1000 次有放回重采样、失败样本跳过和参数矩阵汇总 | 固定种子单元测试、1000 次性能抽查 |
| `visualization.py` | 拟合图、误差棒、残差图、中文字体 | 无界面绘图测试 |
| `report_generator.py` | TXT 报告内容和保存 | 单元测试、端到端测试 |
| `app.py` | Tkinter GUI 和完整操作流程 | 导入测试、窗口创建冒烟测试 |
| `main.py` | 程序入口 | 窗口创建冒烟测试 |

## 五、典型 AI 问题与修正记录

### 1. 最初的数据数量规则表述不统一

- 问题：设计文档一处写“唯一 X 不少于参数数”，另一处写“必须大于参数数”。
- 修正：统一为“有效总点数大于参数数，唯一 X 数量不少于参数数”。
- 验证：`test_data_processor.py` 覆盖边界情况。

### 2. 指数模型最初考虑静默裁剪指数输入

- 问题：裁剪虽然避免溢出，但会改变模型本身，可能产生看似正常的错误结果。
- 修正：改成根据 X 尺度限制参数 `b`，发生溢出时明确报错。
- 验证：拟合引擎边界与四模型集成测试。

### 3. 误差棒最初只考虑 CSV 第三列

- 问题：只有 X、Y 两列的文件无法演示误差棒。
- 修正：增加“无误差、CSV 误差列、统一误差值”三种模式，不使用随机误差。
- 验证：数据处理测试和 GUI 状态测试。

### 4. Matplotlib 默认字体缺少中文

- 问题：测试时图表中文出现缺字警告，实际界面可能显示方框。
- 修正：启动绘图时优先选择 Microsoft YaHei、SimHei 等系统字体。
- 验证：重新运行 27 项测试后无字体警告。

### 5. 随机测试脚本与 Pandas 3 类型规则不兼容

- 问题：测试脚本向 float64 列写入中文“缺失”字符串，Pandas 3 拒绝隐式升级；
- 修正：测试构造数据时显式转换为 object 列；
- 说明：这是测试脚本问题，不是业务程序缺陷；该阶段修正后 46 项测试通过，后续又补充了 GUI 本地化相关检查。

> 下列第 6～14 项为本人发现、复测并确认的开发记录，AI 在其中提供辅助定位、编码或测试支持。

### 6. 参数输入框未说明符号在公式中的含义（本人发现，AI 辅助修正，2026-09-12）

- 现象记录：GUI 截图检查中，参数输入框只显示 `a0`、`a1`、`a`、`b` 等符号，用户无法判断它们分别对应公式中的哪一项，不利于手动设置初值。
- 问题原因：模型元数据只有 `formula` 和 `parameter_names`，没有保存与参数顺序一一对应的中文含义；GUI 也没有显示当前模型公式。
- 根据反馈完成的修改：在 `src/fitting_models.py` 的 `ModelSpec` 中增加 `parameter_descriptions`，为多项式、指数、正弦和 Logistic 分别补充参数说明；在 `src/app.py` 参数区域显示公式，并将输入框标签改为“参数符号（参数含义）”。AI 在其中用于辅助修改代码和补充测试。
- 复核重点：确认多项式采用升幂顺序（`a0` 为常数项、`a1` 为一次项系数），并核对指数、正弦、Logistic 的参数说明与实际函数签名及公式一致。
- 验证：新增 `tests/test_fitting_models.py` 元数据测试，检查参数说明数量、顺序和二阶多项式公式；源码编译和核心元数据检查通过。完整 pytest 测试需由学生在已安装测试依赖的环境中复测。
- 责任说明：问题由本人发现并复测，AI 根据反馈协助修改代码和补充测试，最终界面效果由本人确认。

### 7. GUI 按钮样式和小窗口布局问题（本人发现，AI 辅助修正，2026-09-12）

- 现象记录：启动 GUI 检查时，“智能初值”和“开始拟合”的字体大小、按钮高度不一致；当参数行较多且窗口不是全屏时，底部状态栏会被挤压消失。
- 原因分析：两个按钮分别使用 ttk 默认样式和 `Primary.TButton`；左侧控制区没有滚动容器；此外 `main` 主内容区先 `pack(expand=True)`，状态栏后 pack，导致状态栏在 Tk 的空间分配中被压缩到约 1×1 像素。
- 根据反馈完成的修改：为两个操作按钮统一 `Action.TButton` 字体和内边距；将左侧控制区放入带滚动条的 Canvas，参数过多时仅滚动左侧内容；调整状态栏与主内容区的 pack 顺序，先固定状态栏再扩展主内容区。AI 在其中用于辅助定位和实现。
- 复核重点：在小窗口和 10 阶多项式参数行较多的情况下检查状态栏、滚动条、右侧图表和按钮样式。
- 验证：实际启动并退出 `py -m src.main`；使用 Tk 几何检查模拟 1050×400 和 1050×680 窗口，确认状态栏保持 33px 高度、参数 Canvas 出现有效滚动区域；完整测试 `47 passed`。
- 责任说明：问题由本人发现并复测，AI 根据反馈协助完成布局调整，最终 GUI 效果由本人确认。

### 8. 左侧滚轮误改下拉框选项且滚动不稳定（本人发现，AI 辅助修正，2026-09-12）

- 现象记录：鼠标位于 X 列、Y 列或误差来源等 Combobox 上方滚动时，下拉框选项会被改变；在左侧整体界面滚动时，有时不能正常移动。
- 原因分析：原实现使用 `bind_all` 临时绑定滚轮，ttk Combobox/Spinbox 的默认绑定仍可能同时处理同一事件；普通左侧子控件也没有统一的滚轮优先级绑定。
- 根据反馈完成的修改：为左侧控制区建立专用 bind tag，并递归添加到所有左侧控件的 bindtags 首位；统一处理 Windows `<MouseWheel>` 和 Linux `<Button-4>/<Button-5>` 事件；滚动左侧 Canvas 后返回 `break`，阻止下拉框和数字框的默认滚轮改值行为。AI 在其中用于辅助定位事件传播问题。
- 复核重点：在 X 列下拉框上发送滚轮事件，检查选中值是否保持不变、左侧 Canvas 的 `yview` 是否移动，并检查 10 阶多项式参数区能否连续滚动。
- 验证：事件级 Tk 检查通过，Combobox 值未改变、Canvas 滚动位置正常；完整测试 `47 passed`。最终鼠标滚轮体验仍需学生在目标电脑上分别测试滚轮向上/向下和不同左侧控件位置。
- 责任说明：问题由本人发现并复测，AI 协助分析 Tk 事件传播并实施修复，实际设备操作由本人确认。

### 9. 全屏时不应显示或响应左侧滚轮（本人发现，AI 辅助修正，2026-09-12）

- 现象记录：左侧内容在全屏或大窗口中已经完全显示时，继续保留滚轮区域会增加无效操作；全屏时无需使用滚轮。
- 原因分析：左侧 Canvas 原先始终作为可滚动容器使用，没有根据内容高度和可视高度判断是否真的需要滚动。
- 根据反馈完成的修改：比较 Canvas 内容 `scrollregion` 高度与视口高度；内容未超出时隐藏滚动条、将 Canvas 回到顶部并禁用滚动动作；只有内容超出视口（例如小窗口或高阶多项式）时才显示滚动条并响应滚轮。AI 在其中用于辅助实现按需滚动逻辑。
- 复核重点：检查 1050×800 窗口和低阶多项式下滚动条隐藏、1050×680 窗口和 10 阶多项式下滚动条显示，以及切换模型后的状态更新。
- 验证：Tk 几何和事件检查通过；全屏下拉框滚轮不会改值且 Canvas `yview` 保持不变，小窗口高阶参数滚动正常；完整测试 `47 passed`。
- 责任说明：体验问题由本人提出并复测，AI 协助实现按内容高度切换滚动状态，最大化、还原窗口和缩放场景均由本人确认。

### 10. Matplotlib 工具栏提示为英文（本人发现，AI 辅助修正，2026-09-12）

- 现象记录：GUI 截图检查中，鼠标悬停在图表工具栏的平移按钮上会显示英文提示，例如 `Left button pans, Right button zooms`，与中文界面不一致。
- 原因分析：Matplotlib 的 `NavigationToolbar2Tk` 使用内置英文 `toolitems` 和 Tooltip 文本，项目原先直接使用第三方默认工具栏，没有做本地化覆盖。
- 根据反馈完成的修改：在 `src/app.py` 中定义 `ChineseNavigationToolbar2Tk` 子类，覆盖工具栏的 `toolitems`，将初始视图、前进后退、平移、矩形缩放、子图配置和保存图像等提示改为中文；项目仍继承 Matplotlib 原有按钮和操作逻辑，不修改第三方库文件。AI 在其中用于辅助定位第三方默认文本。
- 复核重点：逐项检查工具栏按钮的中文提示与实际操作是否一致，并确认平移和缩放提示保留必要的鼠标和快捷键说明。
- 验证：实际创建 GUI 后确认工具栏类为 `ChineseNavigationToolbar2Tk`，7 个工具按钮均有中文提示元数据；完整测试 `47 passed`。
- 责任说明：问题由本人发现并复测，AI 协助定位第三方默认文本并完成中文化，工具栏提示由本人逐项确认。

### 11. 子图配置窗口内部仍为英文（本人发现，AI 辅助修正，2026-09-12）

- 现象记录：工具栏 Tooltip 中文化后，检查“配置子图”功能时，发现打开的窗口标题、说明、六个滑块标签和 `Reset` 按钮仍为英文。
- 原因分析：Matplotlib 的 `SubplotTool` 在内部写死 `left/bottom/right/top/wspace/hspace` 标签，并使用标签文本作为 `subplots_adjust` 参数键，不能只替换可见文字，否则会导致滑块功能失效。
- 根据反馈完成的修改：新增 `ChineseSubplotTool`，将显示标签改为“左边距、下边距、右边距、上边距、水平间距、垂直间距”，将窗口标题、顶部说明和重置按钮改为中文；重写滑块回调，以固定英文内部键调用 Matplotlib，分离显示文字和程序参数。AI 在其中用于辅助分析第三方实现。
- 复核重点：打开配置窗口检查可见文字，拖动滑块检查原图布局变化，并点击重置检查是否恢复初始布局。
- 验证：实际创建配置窗口并读取文字，标题、说明、六个滑块及重置按钮均为中文；调整左边距后目标 Figure 的 `subplotpars.left` 发生变化；新增本地化元数据测试，完整测试 `48 passed`。
- 责任说明：问题由本人发现并复测，AI 协助分析第三方实现和完成中文化，滑块操作及重置功能由本人确认。

### 12. 增加中英文 GUI 双向切换（本人提出，AI 辅助实现，2026-09-17）

- 需求记录：程序进入时默认中文；在“帮助/Help”菜单中提供“界面语言/Language”，可切换为 English，也能沿相同路径切回中文；主窗口、菜单、下拉项、状态栏、消息框、参数说明、图表、工具栏 Tooltip 和子图配置窗口必须同步切换。
- 设计原因：原界面文字分散在 `app.py`、可视化模块和 Matplotlib 组件中，模型/误差下拉框又把中文显示值直接作为查找键，直接替换文字会导致业务配置失效。
- 根据反馈完成的修改：新增 `src/localization.py`，集中维护中英文文本、模型/误差内部键映射、参数说明和常用业务提示翻译；扩展 `src/gui_components.py`，根据当前语言创建工具栏和子图配置窗口；`src/visualization.py` 增加语言参数；`app.py` 仅协调语言切换、控件刷新和状态保持。
- 状态保持：切换语言时先保存 `polynomial/exponential/...`、`none/column/...` 等内部键，再更新显示值；CSV、参数输入、智能初值和已有拟合结果均不清空，图表按新语言重绘。
- 复核重点：默认语言为中文；中文→English→中文可双向切换；英文界面下主菜单、四个分区、下拉项、按钮、状态栏、图表标题、工具栏提示和子图配置页均为英文；切换前后的参数值和拟合结果对象保持不变。
- 验证：使用真实 Tk 窗口加载多项式示例、生成智能初值并完成拟合后进行双向切换；参数值和结果均保留，英文/中文图表与工具栏同步刷新；新增本地化映射和双向切换测试，完整测试 `50 passed`，源码编译检查通过。
- 责任说明：双语功能由本人提出并复测，AI 用于辅助拆分模块、实现和补充测试；最终菜单路径、翻译内容和切换体验由本人确认。

### 13. 英文参数说明挤压数值输入框（本人发现，AI 辅助修正，2026-09-17）

- 现象记录：切换为英文界面后，较长的参数含义与参数符号显示在同一行，会占用参数区域横向空间，导致右侧数值输入框变窄，数字可能显示不全。
- 原因分析：英文参数标签和输入框分别位于同一 Grid 行的两列；英文说明通常比中文长，标签列的请求宽度会挤压设置为自动伸展的输入列。
- 根据反馈完成的修改：中文参数布局保持不变；英文参数改为两行显示，第一行只放参数符号和数值输入框，第二行放英文说明并横跨两列、自动换行。AI 用于辅助定位 Tk Grid 的列宽竞争并完成最小布局修改。
- 复核重点：检查多项式 10 阶、指数、正弦和 Logistic 四种模型的英文参数区；确认输入框宽度足以显示数字、英文说明完整、左侧小窗口仍可滚动，并确认中英文切换后手工参数值不丢失。
- 验证：新增英文参数布局 Tk 测试；1050×680 窗口中 10 阶多项式的 11 个输入框均保持至少 100px 宽度，四种模型实测最窄输入框为 157px；英文→中文切换后测试值 `123.456` 保留；完整测试、源码编译和差异格式检查均通过。
- 责任说明：问题和分行建议由本人提出并复测，AI 辅助修改布局和补充自动化测试，最终英文界面显示效果由本人确认。

### 14. 英文界面的 CSV 列名下拉项仍显示中文（本人发现，AI 辅助修正，2026-09-17）

- 现象记录：英文界面的“X Column”“Y Column”和“Error Source”标签已经翻译，但加载含“输入量、输出量、误差”表头的 CSV 后，下拉选项及数据预览表头仍显示中文。
- 原因分析：此前语言切换只覆盖程序自带的固定控件和固定选项，CSV 原始列名被直接用作 Combobox 的显示值和数据处理键，没有区分“界面显示名”与“真实源列名”。
- 根据反馈完成的修改：为常见课程数据表头增加保守的中英文显示映射；英文界面把“输入量、输出量、误差”等显示为 `Input、Output、Error`，中文界面显示原名；拟合和数据清洗仍通过反向映射读取 CSV 的真实列名。误差来源及已打开的数据预览表头也随语言同步刷新；无法可靠识别的自定义列名保持原样。
- 复核重点：检查 X、Y、误差来源三个下拉框和数据预览表头；确认英文显示不改变底层 DataFrame 列名，切换回中文后选择保持一致，并验证自定义或翻译后重名的列不会被错误替换。
- 验证：使用 `random_logistic_unsorted.csv` 实测英文下拉项和预览表头均为 `Input、Output、Error`，数据处理结果仍记录真实列名“输入量、输出量、误差”且成功清洗 62 行；新增列名双向切换、底层映射和未知/重名保护测试；完整测试、源码编译和差异格式检查均通过。
- 责任说明：遗漏问题由本人通过英文界面截图发现并复测，AI 辅助补充显示层映射和回归测试，最终界面由本人确认。

### 15. 增加蒙特卡洛误差分析进阶模块（AI 生成初稿，2026-09-18）

- 需求来源：指导书第 38 题进阶要求第 2 项，要求对拟合参数执行 1000 次重采样并绘制直方图。
- AI 参与内容：比较两个进阶要求的实现复杂度；生成 `monte_carlo.py`、GUI 按钮及回调、参数直方图函数和测试代码；同步整理需求、设计、测试及开发记录。
- 精简选择：复用已有 `CleanedData`、`FitConfig` 和 `fit_curve`；使用 `dataclasses.replace` 构造重采样数据；固定执行 1000 次，不引入新依赖和额外配置层。
- 验证结果：非 GUI 回归测试 `49 passed`；相关目标测试 `10 passed`；实际 1000 次运行返回 `(1000, 3)`，耗时约 `0.273 s`。
- 已知限制：重采样在 GUI 主线程同步执行，大数据或复杂模型可能暂时阻塞界面；当前课程示例运行时间很短，因此未增加线程复杂度。
- 责任说明：本次新增代码初稿由 Codex 生成。本人已阅读 `bootstrap_parameters`，能够解释有放回抽样与失败样本处理，并已实际复核 GUI 按钮、状态提示和参数直方图。

### 16. 蒙特卡洛多参数图表重叠（本人发现，AI 辅助纠错，2026-09-18）

- 人工发现：本人使用高阶多项式复核蒙特卡洛功能时，发现参数较多会使直方图、参数字母和坐标文字重叠。
- AI 定位：原代码固定两列、窗口固定 `850×650`，11 个参数会形成 6 行；`tight_layout` 难以同时为总标题和大量子图留出足够空间。
- 修正内容：AI 辅助将布局改为随参数数量选择 1～3 列，启用 Matplotlib 原生约束布局，减少 X 轴主刻度并在参数超过 6 个时放大结果窗口。
- 验证过程：补充 11 参数 `4×3` 网格回归测试并生成实际 PNG 检查；相关测试 `6 passed`，图中各参数标题、坐标文字和刻度无重叠。
- 责任说明：问题由本人发现并提出，AI 用于定位、最小代码修改和测试补充，最终图形由本人复核确认。

## 六、关键提示词/过程记录说明

完整对话过程保存在 Codex 当前任务中。提交时可根据教师要求截取以下过程：

- 学习课程指导书第 38 题；
- 检查需求分析文档；
- 设计阶段规划与复核；
- 修正数据点规则、指数溢出和误差棒设计；
- 分阶段编码、运行测试和修复问题。

## 七、学生人工验证记录（已确认）

- 验证人：项目作者本人
- 验证日期：基础功能 2026-09-17；蒙特卡洛进阶功能 2026-09-18
- 亲自运行的示例：`data/` 目录四份示例及 `tests/test_data/` 中的异常场景
- 亲自修改的功能：参数说明、GUI 布局与滚轮交互、工具栏/子图配置及中英文切换
- 遇到的问题及解决方法：见本文件第五节及 `docs/开发与Bug修复记录.md`
- 能否解释全部核心代码：是
- 蒙特卡洛人工复核：已完成“拟合 → 蒙特卡洛分析 → 检查参数直方图”，按钮、提示和图形显示正常
- AI 纠错人工复核：已检查高阶多项式的多参数直方图，确认图形与文字不再重叠
